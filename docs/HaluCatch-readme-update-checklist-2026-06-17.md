# HaluCatch README 更新清单

**日期**: 2026-06-17 | **来源**: V4 测试后审查

---

## 必须改（内容错误/过时）

### 1. 「三维度」→「四维度」

**位置**: 第 17 行
```
- HaluCatch 扫描一个 Skill 包，从这三维度给出评级与修复建议。
+ HaluCatch 扫描一个 Skill 包，从地基/代码/规则/护栏四维度给出评级与修复建议。
```

### 2. 代码风险维度描述更新

**位置**: 第 137 行
```
- | 🤖 **代码** | AI 复现篡改点（浮点/异常/p-value） | 代码化扫描 |
+ | 🤖 **代码** | 代码质量风险（路径拼接/静默覆盖/超时缺失/除零等） | 代码化扫描 |
```

### 3. 用例 1 示例更新

**位置**: 第 62-68 行

当前写的是旧金融专属 pattern（skiprows），应改为当前通用 pattern：
```
- **用例 1 — 审查一个数据驱动型 Skill：**
- → 发现：路径硬编码、skiprows 固定为 6、无输入验证。评级：地基 🟠 有隐患。
+ → 发现：字符串拼接路径（建议用 os.path.join）、除法未保护、无输入验证。评级：地基 🟡 有隐患，代码 🟠 有风险。
```

### 4. 「用法」标题重复

**位置**: 第 48-50 行，「## 用法」后紧跟「## 快速开始」

删除第 48 行的 `## 用法`，让「## 快速开始」作为唯一标题。

---

## 建议加（V3/V4 新特性）

### 5. 文件结构图补全

**位置**: 第 116-127 行

```
 HaluCatch/
 ├── SKILL.md
 ├── halucatch_core.py
 ├── README.md
 ├── docs/
 │   ├── decision-flowchart.html
+│   ├── HaluCatch-test-report-2026-06-17.md
+│   ├── HaluCatch-optimization-report-2026-06-17.md
+│   └── HaluCatch-expansion-plan-2026-06-17.md
 ├── tests/
 │   ├── __init__.py
-│   └── test_halucatch.py     ← 单元测试
+│   └── test_halucatch.py     ← 20 个单元测试（6 函数 + 4 边界 + 10 核心）
 └── .gitignore
```

### 6. 四维评估框架补充护栏分层说明

**位置**: 第 131-141 行的表格下方，加一段：

```
> 护栏检查按 Skill 类型分层：数据驱动型检查全 8 项（含数据来源/时效性/置信度），
> 纯方法论型精简为 5 项（跳过不适用项），避免误报。
```

### 7. 补充测试覆盖说明

**位置**: 可在「开发」章节后新增「## 测试」小节：

```
## 测试

```bash
pytest tests/ -v    # 20 个用例全部通过
```

已对 8 个不同类型 Skill 完成实战验证：

| Skill | 类型 | 评级 |
|-------|------|------|
| apple-style-ppt-generator | 方法论 | 🟢 护栏到位 |
| my-knowledge | 方法论 | 🟢 方法论满分 |
| xlsx / pptx | 数据驱动 | 🟢 地基稳固 |
| find-skills / agent-browser / edgeone-deploy / skill-sharpener | 混合 | 🟡~🔴 护栏偏弱 |
```

---

## 可忽略（不影响准确性）

- 用例 2 的「输出格式未定义」→ V4 已修复该误判，但示例本身作为示意仍然合理
- 用例 3 提到的 AI 询问「是否生成骨架脚本」→ 当前版本未实现，可保留为 roadmap
